import { StyleSheet, Text, View, PixelRatio } from 'react-native';
import { colors } from './colors.js';

// Styles used for containers generally throughout the app
export default containers = StyleSheet.create({
   
});